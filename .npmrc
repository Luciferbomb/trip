# Force npm to save exact versions
save-exact=true

# Install all packages listed in dependencies and devDependencies
legacy-peer-deps=true

# Increase network timeout to handle slower connections (5 minutes)
network-timeout=300000

# Use the official npm registry
registry=https://registry.npmjs.org/

# Make sure Netlify can access all needed packages
public-hoist-pattern[]=* 